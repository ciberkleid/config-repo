pivotal:
  app:
    symbols:
      it: EMC,IBM,APPL
      fs: JPM,C,TDA
---
spring:
  profiles: local
  jpa:
     hibernate:
        ddl-auto: update
server:
  port: 8080
---
spring:
  profiles: cloud
  jpa:
     hibernate:
        ddl-auto: update
